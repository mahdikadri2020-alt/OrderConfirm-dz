# Project Rules & Customizations

## Execution Workflow
- When code edits, fixes, optimizations, or feature implementations are requested, execute all file edits, testing, building, and deployment directly without stopping to ask for confirmation first.
- Always perform verification (`npm run build` or relevant tests) after code changes to ensure 100% clean builds.
